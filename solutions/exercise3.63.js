// In Louis's version, every time sqrt_stream_optimized(x) is called,
// it returns a new stream and therefore does not take advantage of
// memoization.
// If Alyssa's approach does not use memoization, it makes no
// difference compared to the original sqrt_stream.
