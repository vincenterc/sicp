// Louis is not correct. The function that Ben claims is sufficient.
// The essential difference between the transfer problem and the exchange
// problem is that the latter reads and updates the account balance.
// If this process is interleaved, result might be incorrect.
