// The order of growth of the original version is
// O(border_size), while for the Reasoner's version, it is
// O(border_size^border_size).
// Therefore, the Reasoner's version takes
// border_size^border_size * T assuming the original version
// solves the puzzle in time T.
